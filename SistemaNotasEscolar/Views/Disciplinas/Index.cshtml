@model IEnumerable<SistemaNotasEscolar.Models.Disciplina>

@{
    ViewData["Title"] = "Disciplinas";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3 mb-0">Disciplinas</h1>
    <a class="btn btn-primary" asp-action="Create">Nova disciplina</a>
</div>

@if (TempData["ErrorMessage"] is string errorMessage)
{
    <div class="alert alert-danger">@errorMessage</div>
}

<table class="table table-striped align-middle">
    <thead>
        <tr>
            <th>Nome</th>
            <th class="text-end">Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var disciplina in Model)
        {
            <tr>
                <td>@disciplina.Nome</td>
                <td class="text-end">
                    <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@disciplina.Id">Editar</a>
                    <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@disciplina.Id">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
