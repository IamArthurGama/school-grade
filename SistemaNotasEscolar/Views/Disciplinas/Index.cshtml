@model IEnumerable<SistemaNotasEscolar.Models.Disciplina>

@{
    ViewData["Title"] = "Disciplinas";
}

<div class="page-header">
    <h1 class="h3 mb-0">Disciplinas</h1>
    <a class="btn btn-primary" asp-action="Create">
        <i data-lucide="book-plus"></i>
        Nova disciplina
    </a>
</div>

@if (TempData["ErrorMessage"] is string errorMessage)
{
    <div class="alert alert-danger mb-3">
        <i data-lucide="alert-circle"></i>
        @errorMessage
    </div>
}

<div class="table-wrapper">
    <table class="table table-striped align-middle mb-0">
        <thead>
            <tr>
                <th>Nome</th>
                <th class="text-end">Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var disciplina in Model)
            {
                <tr>
                    <td>@disciplina.Nome</td>
                    <td class="text-end">
                        <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@disciplina.Id">
                            <i data-lucide="pencil"></i>
                            Editar
                        </a>
                        <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@disciplina.Id">
                            <i data-lucide="trash-2"></i>
                            Excluir
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
